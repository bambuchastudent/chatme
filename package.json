{
  "name": "chatme",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "node --check src/index.js && node --check src/session.js && node --check src/openai.js && node --check src/telegram.js && node --check src/util.js && node --test",
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "openai": "7.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
